<!DOCTYPE html>
<html>
<head>
  <title><% @title %></title>
  <%= stylesheet_link_tag    "application" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
  <meta property="fb:app_id" content="{<%= Rails.application.config.facebook_app_id %>}"/>
  <link rel="stylesheet" href="http://twitter.github.com/bootstrap/1.4.0/bootstrap.min.css">
  <style type="text/css">
  html, body { background-color: #eee; }
  body { padding-top: 40px; }
  .container > footer p { text-align: center; }
  .container { width: 820px; }
  .brand .center{ margin-left: 160px; }
  h1, h2, h3, h4, h5, h6, .brand, p { font-family: 'MuseoSlab500', arial; }
  </style>
</head>
<body>
    
  <div id="my-modal">
  </div>
  <%= render 'shared/header' %>
  
  <div class="container">
        
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>

      <%= yield %>
      
      <div id="friend-box">
        <img src="/assets/close.png" id="close"/>
        <div id="friends">
        </div> 
      </div>
      
    <footer>
      <p>&copy; Marqueed 2012</p>
    </footer>
    
  </div>  
  
  <%= render 'shared/facebook' %>

</body>
</html>
